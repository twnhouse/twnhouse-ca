import{Pool}from'pg';const pool=new Pool({connectionString:process.env.POSTGRES_URL});export default async function handler(req,res){res.setHeader('Access-Control-Allow-Origin','*');if(req.method==='OPTIONS')return res.status(200).end();try{const[tc,lc]=await Promise.all([pool.query('SELECT COUNT(*) FROM tenant_profiles'),pool.query('SELECT COUNT(*) FROM landlord_profiles')]);return res.status(200).json({status:'ok',stats:{tenants:parseInt(tc.rows[0].count),landlords:parseInt(lc.rows[0].count)}});}catch(err){return res.status(500).json({error:'DB error',details:err.message});}}
